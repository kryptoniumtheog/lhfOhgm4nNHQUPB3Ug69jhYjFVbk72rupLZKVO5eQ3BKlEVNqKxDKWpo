<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ff.clock">

    <!-- These can be in the library root -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>

    <application>
        <receiver
            android:name=".AlarmFireReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.ff.clock.ALARM" />
            </intent-filter>
        </receiver>

        <!-- Handles reminder/alarm action buttons -->
        <receiver
            android:name=".ActionReceiver"
            android:exported="false"/>

        <!-- Foreground service keeps ALARM mode alive until user hits Stop/Snooze -->
        <service
            android:name=".AlarmForegroundService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"/>
    </application>
</manifest>